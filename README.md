# ACSLabs - 🚀

A high-performance, SEO-optimized, and fully responsive industrial website built for **ACSLabs**. This platform is designed to showcase 200+ material testing instruments with integrated lead capture systems.

## 🛠️ Tech Stack

- **Framework:** React 19 (Vite)
- **Language:** TypeScript (Strict Typing)
- **Styling:** Tailwind CSS (Mobile-first)
- **Icons:** Lucide-react
- **Forms:** Formspree (Serverless Lead Capture)

## 📁 Project Structure

```text
src/
├── components/
│   ├── layout/       # Navbar, Footer
│   ├── ui/           # StatusModal, BackToTop, SuccessModal
│   ├── features/     # ProductCard, FilterSidebar
│   └── utils/        # ScrollToTop logic
├── data/
│   └── products.ts   # CENTRAL DATABASE (Edit this to update products)
├── hooks/
│   ├── useProducts.ts # Search & Filter logic
│   └── useSEO.ts      # Automatic Metadata management
├── pages/            # Home, About, Catalog, ProductDetail, Contact, GetQuote
└── types/            # TypeScript Interfaces
```
